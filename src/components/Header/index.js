/**
 * Npm import
 */

import React from 'react';
import PropsTypes from 'prop-types';

/**
 * Local import
 */
import './index.scss';

const Header = () => (
  <div id="header">
    je sus dans header
  </div>
 
);

Header.propTypes = {
};

export default Header;
